# 编码规范

- **不写注释**，除非 WHY 不可从代码推断（隐藏约束、特定 bug 绕过）
- **不提前抽象**：三处相似才考虑抽象，一次性逻辑不需要 helper
- **不加计划外错误处理**：只在系统边界（用户输入、外部 API）校验
- UI 变更必须在浏览器走核心路径目测，不能仅凭类型检查通过就报告完成
- 禁止 XSS、SQL 注入、命令注入等 OWASP Top 10 漏洞
